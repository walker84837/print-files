# print-files

A simple tool to print files with a given extension.

## Usage

```bash
node src/main.js --ext <extension> [--gitignore]
```

- `--ext` or `-e`: The file extension to filter by (e.g., .js).
- `--gitignore` or `-g`: Respect .gitignore patterns.
